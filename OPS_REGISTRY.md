# ork-driver — NPU Op Capability Registry

**READ THIS BEFORE reverse-engineering, re-fixing, or building on ANY NPU op.**
The point of this file is to stop re-derivation: every op/chain/handler that has been
tried is listed here with its **status** and the **probe that proves it**. If you think an
op is broken or missing, check here first — it may already be solved, or already known-dead.

## Chain-transition matrix (op→op) — validated by mode_probe campaign 2026-07-21

`ork_chain_table` (ork_ops.c, single-sourced from `ORK_CHAIN_LIST` in ork_npu.h) records how each ordered
op→op transition may combine: `HW` (one PC-chain), `SW` (safe as separate back-to-back submits), `DISALLOW`
(default for unvalidated pairs — run independently). Census: **HW=6, SW=50, DISALLOW=785** of 841.
- **SW** from the `mode_probe A B 0` (fix=none) campaigns: all 49 pairs among {matmul_f16, matmul_i8,
  exp_i16, silu_i16, mul_i16, mul_f16, add_f16} SAFE; plus `matmul_f16→{gelu_i16, add_i16}` SAFE.
- **HW=6** from the chain probes (chain_gu_silu_probe FFN inner + mm_perchan_f16_probe).
- **`matmul → int8-SDP` = DISALLOW (validated HARD-WEDGE, campaign 2 2026-07-21)**: `MM_F16→SILU_I8` hung,
  `MM_F16→GELU_I8` **hard-wedged the NPU** (needed a power-cycle), `MM_I8→SILU_I8` hung (D-state). It's a
  fp16/int8-matmul → int8-SDP mode-switch hazard — whereas `matmul → int16-SDP` is all-SAFE (so the int16
  SDP path is the safe one; prefer it over int8-SDP in chains). **Cost 3 power-cycles to establish; do NOT
  re-run the int8-SDP transition campaign.** The int8-SDP ops are broadly wedge-prone as post-matmul
  separate-submit victims — left DISALLOW-by-default.
- **SW ≠ HW**: separate-submit safety ≠ PC-chain safety. `matmul_i8→mul_i16` is SW (only its *HW* 2-input SDP
  chain hangs). Conversely `matmul_i8→silu_i8` is **HW** (the FFN gate→silu PC-chain, chain_gu_silu_probe)
  even though its *separate-submit* (SW) path hangs in mode_probe — the HW capability is what the table records.
- Pending/uncampaigned: A=int16/fp16/int8-SDP as contaminator (rows 2–14) were not reached; perchan/softmax/
  reshape/rope have no mode_probe harness. Those transitions stay DISALLOW.

**DISALLOW is a driver-config verdict, not a hardware one.** A `fix=none` wedge means the transition is
unsafe with the transition template we CURRENTLY apply (`ork_npu_enter`/`XSPEC` profile + regcmd) — the NPU
may well support it with the right config. Strong evidence for `matmul→int8-SDP`: (a) it is **HW-safe in a
PC-chain** (the FFN gate→silu, `chain_gu_silu_probe`), so the NPU can do it; (b) the int8-SDP ops never got
the transition tuning the int16-SDP ops did — `XP_SDP` was validated "SDP→matmul safe, no reset" *for the
int16/fp16 SDP ops only*. So the separate-submit `matmul→int8-SDP` wedge is a **missing/wrong int8-SDP
transition template**, not a HW limit. **Fix path** (do NOT brute-force fix=none again — it hard-wedges):
run `mode_probe A B 2` (fix=RESET) on a wedged pair; if RESET makes it safe, add that reset/profile to
`ork_npu_enter` for `matmul→int8-SDP` and UPGRADE the cell `DISALLOW→SW`. Treat every DISALLOW as a candidate
driver fix, not a dead end.

## How to read / maintain this registry (probe-verdict anchored)

- **Status is anchored to a probe — enforced at build time.** Every `PROVEN` / `PARTIAL` /
  `DEAD` row must name a probe/test (or explicitly say `(no ... probe)`), and every probe/op it
  cites must exist. `make check-registry` (run by `make all`; `tools/check_registry.sh`, no NPU)
  **fails the build** otherwise — so "a status with no probe" is a compile error, not a red flag
  you have to spot while reading.
- **Status only changes when a probe is re-run.** Don't upgrade/downgrade a row from a
  code reading or a hunch — run the probe, then edit the row.
- **On touching an op:** if you change an op's behavior, re-run its probe and update the
  row in the SAME commit (see `AGENTS.md` §"Op capability registry").
- The `*_WIP.md` docs are **backstory/detail**. This file is the **index of truth**. When
  a WIP doc and this registry disagree, a probe re-run settles it — see CONTRADICTIONS.

### Status enum

| status | meaning |
|---|---|
| **PROVEN** | bit-exact / coherence validated on-silicon by a named probe. Safe to build on. |
| **PARTIAL** | runs without wedging but is NOT fully correct (ratio given), or works only in a narrow regime. |
| **DEAD** | wedges / hangs / abandoned. **Do not use.** The "use instead" column points to the live path. |
| **WIP** | in progress or walk-proof only; not yet a dependable building block. |

---

## matmul output stages (`orki_set_*` regcmd rewriters, `src/npu/<prec>/regcmd.c`)

| name | purpose | status | probe → verdict | gotchas / layout | use / notes |
|---|---|---|---|---|---|
| `set_i8_out8` (847) | int32 acc → int8 requant (banker's round) | **PROVEN** | `i8out_map_probe` → writes **int8-LINEAR** (m·N+n) | identity = mult `0x4000`/shift `14`. **Only valid K≥512** (small-K schedule miscomputes requant). | basis for `set_i8_silu`, `run_i8_out8` |
| `set_i16_out` (861) | int32 acc → **int16** compact output | **PROVEN (standalone), 2026-07-21** | `i16out_fix_probe` → 128/128; `chain_i16silu_probe` → "BRIDGE WORKS — feeds the int16 SiLU coherently" | Output is **LINEAR (m·N+n), NOT EWCUBEH**. Regs: `0x4010=0x20000000` (int16 precision), `0x4038=(s/8-1)|(N/8-1)`, `0x4050=0x36e`, `0x40c0=0x40`. Old `0x4010=0` + N/16 typo = the WDMA-terminal/precision mismatch that stalled it. Env `ORK_I16OUT_*`. | supersedes the stale "set_i16_out wedges" note (Contradiction #1) |
| `set_f16_out` (891) | shim: fp16 OUT_CVT on int8 matmul | **DEAD / wedge-prone** | (no passing probe) — "writes `0x4084=1` WITHOUT FP32TOFP16_EN, fp16 CVT never enabled → hangs fp16 matmul" | — | **use `orki_f16_set_out_fp16in`** |
| `orki_f16_set_out_fp16in` (920) | fp16-in fp16-out stage from vendor capture | **PROVEN 512/512** | `mm_perchan_f16_probe` (composes it via `ork_f16_npu_probe_mm_f16out`) → "CONTIGUOUS fp16 G (512/512)" | default CONTIGUOUS; `ORK_F16_ATOM8` → EWCUBEH atom-8 cube for chaining. `0x4084=0x00010001`, `0x4040=0x53`. | used by `ork_f16_npu_chain_mm_perchan` |
| `set_i8_silu` (996) | SiLU fused into matmul output (PWL LUT, enable 0x1d) | **PROVEN (curve)** | `fused_silu_test` → "VALIDATED bit-exact" | `R` sets BOTH acc→index step AND LUT→out gain. **But as an FFN quality path it's inaccurate (PPL 55 at all gmax)** — see ggml `ORK_FFN_SILU_I16` instead. | curve proven; quality-dead |
| `orki_i8_set_silu32` (5219) | as above, int32 (un-requantized) output | WIP | (no dedicated pass probe) | int32 out | — |
| `ork_ppu_fuse_enabled` (969) | runtime gate for all fused-output paths | **PROVEN gate** | returns 1 only on rk3588, CPU/NEON fallback otherwise (no probe — trivial runtime gate) | no env var | — |

## chain assemblers (PC-chain / doorbell)

| name | purpose | status | probe → verdict | gotchas | use / notes |
|---|---|---|---|---|---|
| `ork_kv_resident_alloc` / `ork_kv_append` / `ork_kv_resident_free` (Tier 12f, `npu/i8/pack.c`) | keep decode K^T/V as RESIDENT packed int8 weights and write only the new key's tile bytes each step, instead of repacking the growing KV cache every token. K^T is `[512,Lmax]` (new key = a new N-column, single N-tile); V is `[Lmax,HD]` (key is the contraction index, multi-tile once `Lmax>KS`). Both alloc'd zeroed so keys past the current length contribute 0. | **PROVEN 2026-09-04** | `kv_append_probe L HD` → append **bit-identical** to a full `ork_i8_mm_pack` repack (`max\|diff\|=0.000000`) at L=512, 1024 **and 2048 (V multi-tile, 2 K-tiles)**, and COHERENT vs CPU fp (rel-err 0.0034-0.0041). | `HD%32`, `Lmax%32`, `Lmax<=nmax` (8192 on RK3588). Local NPU only. Matmuls run at `K/N=Lmax`, **not** the live length — so cost is set by `Lmax`, size it to the context you actually use or grow it in chunks. Caller does the `[1,len]` softmax on the host (or uses the fused chain below). | **This is the gate on ANY on-NPU decode attention.** Per-token repack measured at **169-308x the cost of an append** (`attn_decode_bench_probe`) and is 68-96% of the whole per-step cost — without this the shape is 10-14x SLOWER than CPU and cannot be made to work. |
| **M=1 DECODE economics of the fused attention chain** (`ork_i8_attn_run_rr` / `ork_mm_attn_rr_orkd` + Tier 12f resident KV) | is `[QK^T->exp->reduce,e.V]` fanned RR across cores worth wiring for decode? | **NOT VIABLE end-to-end (2026-09-04). Wired, correct, and DEFAULT-OFF.** | **END-TO-END (`ork_bench`, qwen3-0.6b-f16, 28 layers H=16 Hkv=8 HD=128, FA on both sides, decode tok/s):** P=1024 off **11.31** / on 3.57 (0.32x); P=2048 off **7.69** / on 1.37 (**0.18x**); P=4096 off **7.61** / on 1.38 (0.18x). A 3-5x REGRESSION at every length, worsening with context. Accuracy is fine (`chainrr_biased_probe` at Nq=1: max\|err\| 0.0092/0.0061/0.0053 at Nk=512/1024/2048) — this is purely economics. | **THE MICROBENCHMARK LIED, and this row used to repeat it.** `attn_decode_bench_probe` put the fused chain at 1.36-2.84x from L=1024 up; end-to-end it loses 3-5x. Two causes, both general lessons: **(1) dispatch count** — the probe timed ONE dispatch of 16 chains against one layer's attention, but a real model needs one dispatch PER LAYER (28/token here, Hkv=8 chains of Nq=2), because layer N depends on layer N-1. That fixed cost is paid 28x/token and cannot be batched away. **(2) the CPU reference was still too slow** — even the fp32 vectorisable loop is far slower than ggml's real attention kernels. The tell is in the baseline: CPU decode is FLAT 2048->4096 (7.69->7.61), so attention is a SMALL fraction of CPU decode at these lengths. Little to win, large tax to pay. | **Do not enable expecting a win, and never size a per-layer op from a single-dispatch probe — use `ork_bench`.** Kept default-off, not deleted: the length gate, Tier 12f append and the direct-mode route are sound and independently useful. Economics could change with many more heads per layer, a much lower submit floor, or a chain spanning layers. |
| **the fused FFN chain's per-channel requant concession — MEASURED, and it is NOT the blocker** (`ORK_FFN_CHAIN`, `ork_ffn_chain_on`) | The fused SiLU output stage applies a SINGLE SCALAR requant `R` and cannot express the model's per-row/per-channel scales. This is recorded in-code as "an accuracy concession, gated off by default", and in project memory as the reason the FFN chain is quality-blocked (PPL 22 vs a 9 target). Question: how much PPL does the concession actually cost? | **QUALITY COST IS NEGLIGIBLE (2026-09-04); the chain is blocked on SPEED, not quality.** | `ork_ppl` on real wikitext (`ppl_wiki45k.txt`, 20 x 512-token windows, 10220 scored tokens), FFN-exact-on-CPU vs `ORK_FFN_CHAIN=1`, engagement confirmed via `ORK_VERBOSE` (`[FFN-CHAIN gate] ffn_chain=1`): **qwen3-0.6b-f16** 31.4913 -> 31.5320 (**+0.13%**), wall 105.3s -> 134.9s (**+28%**); **qwen3-1.7b-f16** 13.9679 -> 14.0169 (**+0.35%**), wall 126.5s -> 179.4s (**+42%**). PPL is bit-deterministic here (a repeat baseline reproduced 31.4913 / NLL 3.449712 exactly), so these deltas are signal, not noise. | **Do not spend regcmd RE on per-channel requant — the prize is ~0.2% PPL.** The "PPL 22 vs 9" figure in project memory is a DIFFERENT configuration (all-int8, activations quantized too), not this concession; it does not reproduce as a property of the scalar requant. Also: **do not measure PPL on synthetic text** — a first attempt on randomly-sampled words gave PPL 145 and compressed the delta to +0.34%, because on unpredictable text everything is equally surprising and quantization damage cannot show. Use real corpora. Wall time here is PPL total wall (a proxy); a standardized prefill number needs `ork_bench`. | The chain resolves per-channel int8 weights from the orkpack, so per-channel survives on the WEIGHTS; only the fused SiLU stage's requant is scalar, and empirically that is nearly free. Related dead ends already in-tree: `ork_i16_npu_chain_mm_perchan` HANGS (the int16 2-input SDP keeps BS-ALU active where the vendor fp16 chain SDP has BS bypassed 0x53) and its fp16-template-derived rewrite plus an `ORK_I16_SEQ` 2-submit route are BOTH already implemented — so the RE groundwork exists if the prize ever grows. |
| **M=1 decode weight-DMA: what the hardware gives vs what the FFN handler gets** (`tools/decode_bw_probe.c`) | Dispatch is NOT the M=1 decode wall — `ORK_FFN_PROF` puts quant+silu+dequant at 48us of 1819us/layer (2.6%); it is all weight DMA. So: what bandwidth does the NPU actually reach at M=1, and how far off is the handler? | **PROVEN: the NPU is NOT bandwidth-lost at M=1. The handler is 1.75-2.4x off its own shapes.** | `decode_bw_probe K N reps` batches S independent M=1 matmuls into one chain. K=1024 N=3072 (gate/up shape): S=1 **8.62 GB/s**, S=2 12.74, S=3 15.05, S=6 **19.87**. K=3072 N=1024 (down shape, same 3MB): S=1 **8.81**, S=3 15.08, S=6 20.17. Ceiling ~22 GB/s; CPU achieves ~9.4 GB/s effective (63.7ms/token over ~600MB int8). Handler measures Gate+Up 872us **7.2 GB/s** and Down 890us **3.5 GB/s**. | **Three hypotheses for the handler's gap are DISPROVEN — do not re-try them.** (a) *wide-K*: down's exact shape reaches 8.81 GB/s in isolation, so K=3072 crossing the ks=2048 slice threshold is not it. (b) *missing `Bf`*: `ORK_NO_BF=1` still gives S=1 8.28 GB/s. (c) *submit mechanism*: the probe's S=1 short-circuits to the same `ork_i8_mm_run` the handler calls. **Cause of Down's 3.5 vs 8.8 is UNKNOWN and is the largest remaining prize (2.4x on a third of the FFN).** Leading untested idea: the probe's tight loop pipelines consecutive submits (S=1 8.7 -> S=3 15.3 means 3 tasks in 619us vs 1083us serial, a 1.75x concurrency win) while the handler interleaves host work and starts each submit cold. | **`ORK_NO_BF=1` DECLINES the batched chain entirely (S>=2 rc!=0)** — so any batching plan requires Bf on the weights. Also corrects a figure: the handler's 3.5/7.2 GB/s is NOT "~3.5 GB/s per task" (which would make dense decode hopeless); one task gets ~8.7 and the aggregate scales sublinearly to ~20. **Decode end-to-end still loses** (5.58 vs 15.70 tok/s CPU on qwen3-0.6b) — the claim here is that the reason is implementation, not bandwidth. |
| `ork_i8_dyn_begin_seq` / `_mc` (10430/10332) | **the working chain assembler** — heterogeneous [matmul+SDP…] as ONE self-healing NONBLOCK doorbell | **PROVEN** | `sdp_chain_probe` → "int8 SDP rides the doorbell (Stage 4). PASS" | TERMINAL op MUST be a matmul (int32 sentinel gates completion); int8-SDP-terminal needs a **B2 witness matmul**; ping-pong OFF; **has a `SILU_I16` kind** (single-core + LUT prologue). | **preferred production chain path** |
| `ork_submit_seq` (12899) | sequence scheduler: batches HW-eligible ops into doorbell submits, SW-breaks rest | **PROVEN** | `test_submit_seq` → heterogeneous seq bit-exact (i8↔f16↔i4) | `seq_hw_ok`: int8/fp16 K%512 & K≤4096, M≤64, Sn==1; int4 M==1, Sk≤16. `SILU_F16` row = NULL (dead, see #4). Routes to `ORKD_SEQ` if daemon up. | — |
| slice-and-dice `ork_mm_pack_sliced` / `ork_mm_run_sliced` / `_free_sliced` (npu.c 5259+) | pack ANY matmul as a set of `c_base` doorbell tiles (K-slice + N-tile via [[ork_slice.h]]) so a wide-K / wide-N op runs ENTIRELY on the doorbell in ONE chained submit — K-slice int32-accumulate + N-tile scatter. Pack once, run many. **Precision-general surface**: dtype-carrying handle, pack/run dispatch per precision. **#33: now WIRED as the auto-RESCUE at every int8 `ORK_RC_WEDGE_PRONE` refuse site** — pack() pre-builds tiles for the refuse-prone shape (Sn>1 && (K>4096\|\|!Bf)), the refuse site runs them, else refuses. | **PROVEN (bit-exact int8; auto-rescue wired; perf-matched to native)** | `slice_dbrun_probe K N M nc` → BIT-EXACT vs `ork_i8_mm_run` at qwen3-1.7b shapes (single-tile 2048², wide-K 6144×2048 nc=1 AND nc=0, wide-N 2048×16384, combined 6144×12288, M=1 decode) — 0 mismatches. **`test_slice_rescue` (in `make test`)**: native==CPU==forced-rescue bit-exact on base + a real Sn>1&K>4096 shape (proves the pack-build + refuse-site plumbing). | **(1) Partial-output buffer MUST be a PLAIN malloc, NOT `ork_dma_alloc`.** A DMA buffer is zero-copy-eligible (`direct` mode); allocated AFTER the weight pack (as a pack-once primitive must), its zero-copy output IOVA/warm state is stale → the NPU writes nowhere → reads back **0** (non-deterministic scattered mismatches, worse multi-core). A plain buffer forces the doorbell scratch copy-back path (`direct=0`, NPU→in-domain `mcc`→memcpy), bit-exact for any M/nc (ZC-OUT is ~0 gain anyway). **(2) ONE chained `ork_dyn_begin_mc(S=all-tiles)` submit** (not `nks*nnt` begin/end round-trips) — the wedge-safe mirror of the mcworker CHAIN-KSPLIT, weight streamed in one pass. **(3) int8 + int4 rescues BUILT; fp16 has no refused shapes (no tiled path).** `ORK_DT_I8`: the rescue path (perf-matched, below). `ORK_DT_F16`: **no fp16 tiled path — fp16 has NO refused shapes.** Out-of-envelope fp16 returns `ORK_RC_F16_SC` → `run()`'s single-core reference (a WORKING fallback, never `ORK_RC_WEDGE_PRONE`), so there is nothing for a rescue to rescue. Its multicore FIT is the existing CONTIG colsplit (`run_multicore` routes all wide fp16 there — wide-K via the Sk-slice loop + lockstep, wide-N via Sn; verified fp16 K=8192 correct at single-core-ref parity). The distinct-tile fp16 twin was the wrong transform (distinct fp16 weight tiles SERIALIZE across cores — `ORK_DYN_DEBUG` nc=3 but wall=S×per-tile, 6-17× slower) AND redundant → REMOVED. `pack_sliced(DT_F16)` returns NULL. **PADDING (int8):** an unaligned K (K%kmul!=0) is padded UP to kmul with zero rows (A zero-padded to Kpad at run) — zeros contribute 0 to the int32 sum, so bit-exact for the real K; `test_slice_rescue` pad-K (1408→1536) + pad-refuse (4224→4608, also Sn>1&K>4096) prove it. N is never padded (real int8 weight is N%32==0). `ORK_DT_I4`: **BUILT** — `slice_pack_i4`/`slice_run_i4` decompose a refused int4 shape into BCHAIN-legal sub-tiles (Sk==1, Sn==1, N%64==0, K≤8192 for BCHAIN H≥2; K-slice 8192, N-tile 8192, K-pad to 32) and run EACH via `run_i4_bchain_db` (M≥2) or the per-row doorbell (M==1), int32 K-accumulate + N-scatter (int4 C is int32 after BCHAIN's de-tile → reuses `slice_acc_worker`). Wired at `ork_i4_mm_run`: for **M≥2 wide-N/wide-K** (Sn>1 or K>8192 — where pack built `w->sliced`) it routes STRAIGHT to the rescue BEFORE `run_i4_mc_db`, and also as the fallback on `run_i4_mc_db`'s -4. Reframing (measured): `run_i4_mc_db` does NOT hard-refuse wide int4 — it RUNS it **per-row, ~6-20× slower**; the rescue is really a wide-int4-prefill ACCELERATOR. Measured M=128 N=16384 K=2048: per-row **770 ms → 33 ms** (BCHAIN-tiled, 2 N-tiles). (Degenerate tiny-K wide-N e.g. K=512 N=16384 stays ~92 ms — BCHAIN's fine bank-tiling at small K, not the decomposition; real int4 K≥2048.) True -4 (ork_i4_dyn_begin_mc NULL) is rarer/pathological. `test_slice_rescue` int4 (fnv64 goldens, ORK_FULL_REF vs CPU-int32 bit-exact): i4-base/wideN(2 N-tiles)/wideK(2 K-slice-accum) + i4-refuse (M=128 natural→rescue rc=0). **Grouped int4** (`ork_i4_mm_run_grouped`, float per-group drain) genuinely HARD-refuses (`ork_i4_dyn_begin_mc_grouped` NULL at per-core programs `(M/nc)*Sn*Sk > ~70`, no internal M-tile) — a TRUE rescue, not a slow-path. Rescued in-place by **M-CHUNK recursion**: rows are independent, so re-call `ork_i4_mm_run_grouped` with fewer rows (each chunk fits Pcore≤cap) — no repack / scale-slice / scatter / float-accumulate (all groups+N kept intact per chunk; contiguous C rows). Covers `Sn*Sk≤cap` (a single row fits); wide-N (Sn>1) or fine-group large-K (Sn*Sk>cap at 1 row) still refuses → N-tile / K-group-slice follow-on. `test_slice_rescue` i4g-refuse (M=64 K=2048 G=128 → Pcore~352 refuse → M-chunk, dequant maxerr<0.05 EXACT). int8/int4 require K%512/N%64 resp.; int8 N%16. **A/B (`slice_dbrun_probe`, 30 reps warm, vs native `ork_i8_mm_run`, all bit-exact) AFTER #33 stage 3 (balanced N-tiling + per-core parallel ks-outer NEON accumulate):** wide-K 6144×2048 M256 **1.32×** (wide-K host-accum DRAM-BW floor, #37), 8192×2048 M128 **1.04×**, wide-N 4096×8704 M64 **1.04×** (was 2.31× — the nmax+remainder tiling put 8192+512 on 2 cores; balanced equal tiles ≥cores fixed it), **7B ffn_down 18944×3584 M64 = 0.95× (BEATS native)**. So the rescue now runs at-or-near native parity, no longer a throughput loss. | foundation for the doorbell owning EVERY submit (SLICE_AND_DICE_PLAN.md); #33 int8 rescue |
| attention seq subset: `REDUCEMAX_I8` / `MUL_PERCHANNEL_F16`+`_I8` / `RSQRT_I16` / `ROPE_NEOX_F16` / `RMSNORM_F16` seq adapters (task #20) | wire the softmax/RMSNorm/rope primitives into `ork_submit_seq` (SW-break SDP dispatch) | **PROVEN** | `softmax_seq_probe` → each adapter dispatches vs CPU (reducemax bit-exact, mul_perchan 2.4e-4, rsqrt_i16 137 LSB RKNN-class, rope 8.8e-4) + end-to-end on-NPU softmax `row_max→exp→Σ-reduce` COHERENT (max\|err\| 1.8e-4) | `RSQRT_I16` appended to enum (i8 pre-existed). `ROPE_NEOX_F16`/`RMSNORM_F16` seq ops = SW-break only (chain transitions DISALLOW, no mode_probe); rope pos via `o->B`+freq_base via `o->in_scale`; rmsnorm gain via `o->B`+eps via `o->in_scale`. Also route through **orkd `ORKD_SEQ` Path B** (npu.c sizing switch extended; daemon reconstructs generically → its own Path A) — `orkd_seq_probe` batches all 6 SDP ops in ONE round-trip, bit-identical to direct | — |
| full attention layer as an `ork_submit_seq` pipeline (task #20) | assemble RMSNorm→QKV→rope→QK^T→softmax→A·V→O→residual as a 13-op seq | **PROVEN (coherent, direct mode)** | `attn_chain_probe` → full single-head layer COHERENT vs CPU (max\|err\| 6.2e-4, mae 9.6e-5, 13 ops rc=0); `attn_chain_probe` core-only (QK^T→softmax→A·V) also validated | Direct-mode coherence only. Intermediates flow through HOST buffers; each dtype bridge (int8-quant, x−max, int16→f16, 1/Σ) + runtime K^T/V densify+pack is a CPU step between ops that blocks batching. orkd `ORKD_SEQ` Path B now carries the attention SDP ops (validated, `orkd_seq_probe`). **Full chain ALSO validated through orkd** (`attn_chain_probe` under `ORK_USE_ORKD=1`): all 7 fp16 matmuls become daemon-resident automatically via `ork_f16_mm_pack`→`orkd_pack_f16` (static Wq/Wk/Wv/Wo/ones AND runtime-packed K^T/V), the 6 SDP ops route Path B → 13-op chain COHERENT, bit-identical to direct (max\|err\| 6.2e-4). Stable across 30+ runs; ONE early transient (~82% off + one op rc≠0) did not reproduce in 28 retries. Matmuls here are SW-stream (K non-conforming, not %512) so they do NOT ride the doorbell's cold-output clean — the matching documented fragility is the int16-LUT lazy calibration wedging as first-op-after-multicore-matmul. HARDENED: orkd startup `orkd_warmup` runs a tiny `exp_i16` (clean int16-LUT calibration, before any matmul) + fp16 matmul, so serving is never cold (logs `warmup rc=0`; disable `ORKD_NO_WARMUP`). NOTE: each op is still its own round-trip (CPU dtype bridges block batching); one-round-trip resident chain needs on-NPU bridges (f32→f16, x−max, requant) | — |
| A2 on-device intermediate residency (ORKD_SEQ v3, task #20) | back-reference: a batched op's A/B aliasing a prior op's C ⇒ consume that resident output on-device (skip re-upload, `a_src`/`b_src`) + keep it resident, not shipped (`c_keep`) | **PROVEN** | `orkd_resident_probe` → batched `rmsnorm→Q/K/V` with `xn` aliased across the 3 matmuls: COHERENT via orkd (max\|err\| 6.2e-4, xn uploaded 0×/never returned). Non-aliasing seqs byte-identical (regression: `orkd_seq_probe`, `attn_chain_probe` still pass) | Zero public-API change — detected by pointer-aliasing in `ork_submit_seq` Path B (same aliasing the direct path already needs); proto bumped v2→v3 (`a_src`/`b_src`/`c_keep` in `struct orkd_seq_op`, default 0 = pre-v3). Reach limited to already-adjacent (bridge-free) op pairs until A1 closes the dtype bridges | — |
| A1+A2 payoff: resident `rmsnorm→QKV→rope` front (task #20) | 6-op contiguous chain — RMSNorm → Q/K/V via `MM_F16_F16OUT` (f16 out) → rope(Q),rope(K); `xn`/`Q`/`K` aliased resident | **PROVEN** | `qkvrope_probe` → COHERENT (max\|err\| 1.977e-3) direct AND via orkd, bit-identical. All fp16, every transition bridge-free (rmsnorm f16→matmul; matmul **f16-out**→rope f16-in — the A1 bridge). Under orkd only x/gain/pos in + V/Qr/Kr out = **ONE round-trip vs 6** per-op | fp16 needs an abs error floor (~4e-3) over a multi-op chain, not pure relative. The attention layer front is now a single resident submit | — |
| A1+A2: resident attention layer BACK `O proj → residual` (task #20) | `O = attn_out · Wo` (`MM_F16_F16OUT`, f16 out) → `y = x + O` (`add_f16`), `O` aliased resident | **PROVEN** | `oresidual_probe` → COHERENT (max\|err\| 8.4e-4) direct AND orkd, bit-identical. Same bridge-free f16-out-matmul→add recipe as the front; ONE round-trip vs 2. | Attention layer FRONT (rmsnorm→QKV→rope) and BACK (O→residual) are now BOTH single resident submits; only the MID softmax island (mixed-dtype, multi-session) remains for a fully-resident layer | — |
| whole decode layer as ONE op: `ork_i8_mm_layer` / `orkd_layer_i8` (task #20; lib↔orkd parity) | rmsnorm→QKV→q/k-norm+rope→attn→o→+res→ffn-norm→gate/up→silu-glu→down→+res; CPU glue (`spine_kernels.h`) interleaved with M=1 matmuls, fp32 I/O | **PROVEN (coherent)** | `orkd_layer_probe` → whole layer via ORKD_LAYER bit-exact (rel 2.03e-07) direct AND daemon; `test_api_parity` → the lib and orkd op sets + shapes are locked in lockstep (compile-time) | SINGLE shared compute core in `npu.c`: transparent `c->daemon`→`orkd_layer_i8` else local; the daemon's `handle_layer` is a thin shim calling the SAME fn (its ctx has no daemon → local path) — that is the lib↔orkd parity guarantee. DEFAULT `run_i8` (the M=1 doorbell MISSES at layer dims — see #13/#21; `ORK_LAYER_DOORBELL=1` opts back in for debugging). **Decode-on-NPU is a MEASURED loss vs CPU (3.25 vs 13.3 t/s); this exists for parity/correctness, NOT as a perf path.** | — |
| CPU↔NPU spine dispatcher: `ork_spine` (include/ork_spine.h) | header-only heterogeneous op-DAG scheduler across an NPU unit (own worker thread, single-stream) + N CPU worker units; per-op placement (NPU/CPU/EITHER) + deps bitmask, one poll loop; cross-unit `dc civac` coherency contract | **PROVEN** | `test_spine` (in `make test`; NPU op = blocking `run_i8`, deterministic) → DAG coherent + overlapped (`wall<serial`), 5/5 runs; `spine_sched_probe` (board tool, RE origin) → same DAG with the nonblock-doorbell NPU op (intermittent doorbell miss, tasks #13/#21 — why the gating test uses `run_i8`) | Header-only (C + C++), lifted from `spine_sched_probe`. ≤1 NPU unit enforced by `ork_spine_run` (single-stream); deps = `1<<idx` bitmask (≤32 ops). GENERAL infra (prefill CPU-quant∥NPU-matmul, mixed CPU/NPU graphs, domain-wall latency bridging) — NOT decode (decode-on-NPU is a measured loss). First perf application deferred (tasks #27/#28). | — |
| A1 fp16-output matmul: `ORK_OP_MM_F16_F16OUT` / `ork_f16_mm_run_f16out` (6005) | matmul emits CONTIGUOUS fp16 (not f32) via the proven `orki_f16_set_out_fp16in` vendor stage, consuming a packed resident `ork_w` (`w->Bb`) | **PROVEN** | `f16out_probe` → matmul-alone bit-exact vs f16-narrowed CPU; `MM_F16_F16OUT→mul_perchan` with intermediate aliased = bit-exact direct AND via orkd (A1+A2 together, no f32→f16 bridge) | Single-tile (M≤64, single-slice), single-core, own submit ⇒ SEQ_CLASS hw=0 (SW dispatch, not the f32-out doorbell). Kills the pervasive matmul→SDP f32→f16 bridge; e.g. `MM_F16_F16OUT→rope`/`→mul_perchan` become adjacent+resident. | — |
| A1 exp→Σ bridge: symmetric int8 reduce (path B) | `exp_i8`(int8 out)→`MM_I8`(e·ones_i8)→int32 Σ instead of `exp_i16`→[int16→f16 cast]→`MM_F16` | **PROVEN** | `int8reduce_probe` → Σ within 0.9% of CPU exp-sum (max rel-err 9e-3, 0/64 rows>3%), bit-identical direct AND orkd (e_i8 A2-resident). out_scale cancels in the normalize so int8 is fine for Σ | **KEY HW FACT: RK3588 is symmetric-precision (README:105 "all mixed types reject at create") — a single int16-in/fp16-out op is a DEAD END (verified: my hand-built `0x4010=0x44000002`+FP32TOFP16 cast got ETIMEDOUT/rejected, self-healed). Bridges must stay same-precision; int8 reduce is the symmetric answer. **x−max broadcast-sub (last A1 bridge) — INVESTIGATED, not landed:** needs a per-channel ADD (bias=−max) in the transposed layout, but the ERDMA/EW_ALU path is MUL-only — only the `EW_MUL` stage broadcasts operand-b per-channel (proves `mul_perchan`); the `EW_ALU` add stage does not (swept 0x5034 modes: 0x08 best but 69% wrong, 0x40000008 99% wrong, 0x80/0xc0000008 reject). The MUL/ADD f16 templates differ ONLY at `0x4070` (EW_MUL vs EW_ALU=SUM). **CLOSED via matmul-broadcast + add** (proven ops, zero register RE): `-max_bc = MM_F16_F16OUT(max_padded[M,32] · -ones[32,N])` (K=32 outer product, col0=max ⇒ every element = -max[m], fp16 out) then `add_f16(scores, -max_bc)` = scores-max. `xmax_probe`: COHERENT direct (max\|err\| 1.95e-3) AND via orkd as one aliased 2-op seq (A2-resident: -max_bc fed forward on-device). NOTE: the earlier "add_f16 garbages" was a PROBE bug (`(ork_f16)f2h(x)` bit-cast into native `_Float16` → huge/inf operands overflowed + stalled add's inf-poison poll) — `add_f16` and the transition are fine. The BS_ALU/ERDMA per-channel-add path was abandoned (unproven + wedge-prone); the matmul-broadcast is strictly better | — |
| softmax exp — HW-CHAINED via fused output-stage (M4.8, `ork_f16_mm_run_act` fn=exp) | `C = exp(Q·Kᵀ)` — exp rides the score matmul's DPU output stage in ONE submit (no separate exp op, no matmul→SDP crossing) | **PROVEN** | `fused_exp_probe` → COHERENT (max\|err\| 1.45e-4) one submit, scores≤0 (post-max, single-signed fp16 index). **Chosen over the transposed int8/int16 softmax: HW-chained (1 submit vs many) + exp free + comparable accuracy.** | **M4.8 is a general technique** — HW-chains any `matmul→pointwise-LUT` (exp/silu/gelu/rsqrt): softmax exp on QK^T, FFN silu on gate-proj, RMSNorm rsqrt on the reduce. Does NOT apply to non-pointwise transitions (rope, residual-add, per-channel scale — those stay A1 f16-out + A2-resident, or the output-stage per-channel path). Full resident softmax = fused-exp(QK^T) + Σ-reduce-matmul + normalize; still needs the max-subtract (global-max scalar bias, or the on-NPU x-max matmul-broadcast). Follow-up: packed-`ork_w` fused-exp for seq-composition | — |
| (a) full fused-exp softmax island + (b) fused rsqrt/silu (task #20) | softmax = fused-exp(QK^T) + on-NPU Σ-reduce(e·ones) + normalize; shared M4.8 fused-act also does RMSNorm rsqrt + FFN silu | **PROVEN** | `fused_softmax_probe` → softmax COHERENT vs CPU (max\|err\| 8e-3, mae 1.5e-3, exp HW-chained one submit + on-NPU reduce). `fused_act_probe` → rsqrt COHERENT (1.8e-4), silu within fp16 bound. | The softmax problem is solved via the fp16 fused-exp path (no int8, no f16→int8 quantize — that was the wrong path). Full one-round-trip residency needs the packed-`ork_w` fused-exp as a seq op + on-NPU max-subtract (the x-max matmul-broadcast) — all pieces proven, remaining work is composition | — |
| pack-once / run-many resident fused activation (`ork_f16_mm_pack_fused_act` + `ork_f16_mm_run_fused_act`, npu.c 5492+) | split of `ork_f16_mm_run_act`: pack bakes the calibrated PWL LUT + out-scale INTO the weight (`w->fa_lut`/`w->fa_osc`); run replays `C=fn(A·B)` in one submit with NO re-pack. `run_f16_act` is now the thin one-shot wrapper (pack+run+free) over the same path. | **PROVEN** | `fused_resident_probe` → exp packed ONCE, run TWICE on different Q against the SAME resident weight, both COHERENT (max\|err\| 1.6e-4, 0/2048). | The enabler for composing fused exp/rsqrt/silu inside a RESIDENT seq — the per-call LUT-rebuild+re-pack in `run_f16_act` would defeat residency. Run path carries no `fn` pointer (baked into the weight) so it crosses a seq/socket. Direct-mode proven. **The fused-exp→reduce handoff to make it a resident softmax seq is the open piece — see next row.** | — |
| f16-OUT on the fused-LUT path (fused exp → fp16 out for a bridge-free reduce handoff) | override the DPU output CVT (0x4010=0x48000002 fp16→fp16 + 0x4084 FP32TOFP16_EN) while keeping the LUT stage active, so `C=fn(A·B)` writes CONTIGUOUS fp16 the Σ-reduce consumes resident (out-scale cancels in P=e/Σ) | **DEAD (HW-rejected)** | (no fused_f16out probe — the trial probe was reverted): RKNPU_SUBMIT ETIMEDOUT (errno 110) + self-healing reset; bailed at S1 before the reduce, board recovered clean | **STRUCTURAL, not a wrong constant — do NOT re-sweep.** The proven fp16-out stage (`orki_f16_set_out_fp16in`) needs BS(0x4040=0x53)/BN(0x4060=0x53)/EW(0x4070=0x383) FULLY BYPASSED, but the LUT rides those exact stages (`set_f16_silu` sets 0x4060/0x4070 active) → fp16-out CVT + active-LUT-stage = proc-precision mismatch (same dead class as the int16→f16 cast, README:105 symmetric-precision). The fused-LUT output stage is fp32-out ONLY. **Resident fused-exp softmax needs a different handoff** (options: on-device f32→f16 narrow op between exp and reduce; or use the ALREADY-PROVEN int8 exp→Σ symmetric reduce — `int8reduce_probe` — as the resident softmax island instead of the fused-fp16 exp). | — |
| `MATMUL_I16OUT_I8` seq op (`ork_i8_mm_run_out16`, int8 matmul→int16 out via `set_i16_out`) | int32-acc → int16 COMPACT-LINEAR output, fed to an int16 SDP op resident (A2) instead of hardware PC-chaining | **PROVEN (direct)** | `i16out_seq_probe` → S1 int16 matmul bit-exact (0 LSB); S2 `MM_I8_OUT16→exp_i16` resident == S0 standalone `exp_i16` (63 LSB, identical) ⇒ **A2 resident-forwarding sidesteps the `set_i16_out` PC-chain issue** (LINEAR int16 out IS the contiguous host layout the int16 SDP reads — no cube bridge, no chain fragility). | K≥512 (int8 requant; K=128 rejects → NOT usable for softmax head_dim). orkd Path B returns -2 (daemon-resident weight lacks `w->Bf` that `run_i8_out16` needs — orkd-weight follow-up). Generalizes the specialized `gatesilu` hardware chain to any int8-mm→int16-SDP via the seq | — |
| K-padding sidesteps the small-K int8-requant wall (task #20 high-effort path) | zero-pad the contraction dim (head_dim 128 → 512): the 384 padded rows are `0·0=0` terms that don't change the sum, so a K-padded int8 QK^T is EXACT and rides the PROVEN K=512 requant path — no schedule RE, no wedge risk | **PROVEN** | `kpad_qkt_probe` → int8 QK^T at d=128 (padded to 512) vs CPU over d=128: max\|err\| 1 LSB (requant rounding), 0/4096 off | Reframes crack (c) from "risky schedule RE" to a zero-RE sidestep. Cost: 4× contraction MACs (cheap for int8/short context). **Unlocks the all-int8 resident softmax FRONT: int8 QK^T → int8 scores at real head_dim.** Remaining for a fully-resident int8 softmax: bake a GLOBAL-max scalar bias into `exp_i8` (per-row max needs the dead per-channel-add; global max is a scalar → bakeable, and correct+stable since the constant cancels in P=e/Σ) → `exp_i8`→`MM_I8` reduce (PROVEN, `int8reduce_probe`) → deferred normalize (fold 1/Σ into A·V). Open: scalar-max-bias into exp_i8; int8 score precision for softmax quality | — |
| scalar global-max-biased `exp_i8` (`ork_i8_npu_exp_biased`, `act_lut_i8_biased`/`silu_build_curve_biased`) | bake a scalar max-subtract into the exp LUT: `out = clamp_i8(exp((x-max)*in_scale)/out_scale)`, max = the scalar GLOBAL max | **PROVEN** | `exp_biased_probe` → softmax `P=e/Σ` COHERENT vs CPU (max\|err\| 1.9e-3, 0/2048); plain `exp_i8` saturates 989/2048 (48%) on the same scores (why the subtract is needed); biased exp 3.2 LSB (int8-LUT class) | The numerically-stable softmax numerator WITHOUT a per-row op (per-row max needs the dead per-channel-add; a scalar global max ≥ every row max so every arg ≤0 → exp∈(0,1], no int8 overflow — and the constant cancels in `P=e/Σ`). For a single-submit resident chain the bias must be a STATIC calibrated bound (runtime gmax = a 2nd pass), tight-calibrated to avoid int8 underflow. Direct only (Path B TODO). Pairs with [[K-padded QK^T]] + `exp_i8`→`MM_I8` reduce (`int8reduce_probe`) → the resident int8 softmax pieces are ALL proven; remaining = assembly into one seq + on-NPU A·V connection | — |
| round-robin step 1: core-parameterize the fused chain (`ork_npu_set_chain_core`) | thread a target core through `run_chain_i8_*`: the LUT-load + program submit both run on `chain_cc` (was hardwired core 0); per-core LUT-load tracking (`chain_lut_core`). Foundation for running independent chains on the 3 cores concurrently. | **PARTIAL (core 0 safe; non-0 WEDGES — clamped)** | `chaincore_probe`: core 0 COHERENT (0/4096). **Running the chain on core 1/2 HARD-WEDGED the NPU (physical power-cycle required, 2026-07-23).** | **FINDING: core-param is COUPLED to per-core scratch (step 2) — you can't split them.** Only `core_mask` was moved; the chain's scratch + warm (`c->regcmd`/`task`/`Af`/`Cc`/`cres`, warm flags) are still core-0-oriented, so submitting on core 1/2 against them wedges. Core 0 is behavior-identical to before (verified COHERENT). Until step 2, `ork_npu_set_chain_core` CLAMPS non-0 to 0 (warns) so it can't wedge. **Step 2 = per-core scratch buffers + per-core warm; then non-0 unlocks and 3 chains run concurrently (each core its own LUT-sticky lease).** | — |
| **HW-chained attention FRONT `[Q,K,V = xn·W]` in ONE submit + the 2-chain layer architecture** | `run_chain_i8([Wq,Wk,Wv])` batches the 3 projections into one coalesced submit. Full front would prepend RMSNorm (x²→reduce→rsqrt→scale — a rsqrt-curve chain). | **PROVEN (direct)** | `chainqkv_probe`: rc=0, ONE submit, Q/K/V bit-exact vs CPU (0/4096). | **ARCHITECTURE: the attention layer is inherently ≥2 HW chains + a host bridge, NOT one chain.** FRONT `[RMSNorm→QKV]` → **host bridge (rope + K^T transpose/pack)** → CORE `[QK^T→exp→reduce,e·V]`. Two walls prevent merging front into the core: (1) QK^T needs K as a STATIC packed weight, but K is dynamic (`xn·Wk`) — the chain has no dynamic-tensor-as-weight matmul, so K^T must be transposed+packed on the host; (2) rope is not a chain kind. Also the one-fused-curve-per-chain limit means RMSNorm(rsqrt) and softmax(exp) can't share a chain. So: front-chain + core-chain, each one submit, bridged on host. **Making the K^T/V pack ON-DEVICE (to drop the host bridge) = building an op that writes a dynamic activation into `ork_i8_mm_repack`'s 32×32-tiled-transpose weight-cube. Investigated (2026-07-23): this IS the vendor RESHAPE op (`regcmd_reshape.h` / `RESHAPE_WIP.md`) — the ONLY on-NPU contiguous→cube reformat (the fp16 matmul HANGS writing the cube; int8 matmul writes LINEAR only). That reshape is an active-but-BLOCKED RE campaign (task4 base decoded; read-geometry stride formula `0x107c=32`/`0x1080=0x0fffffe8` undecoded, needs multi-shape re-capture WITH weight-buffer contents). So the on-device bridge is gated on the reshape decode, not a quick build.** Alternative (unexplored): a dynamic-B int8 matmul (weight-fetch reads a linear K activation, no pack) — a separate weight-fetch-geometry RE. | — |
| **method (b): `scores^T = K·Q^T` operand-swap — QK^T with K as the ACTIVATION (no K^T pack)** | compute the transpose of the score matrix: `matmul(A=K_pad[Nk,512], W=pack(Q^T[512,Nq])) → scores^T[Nk,Nq]`. K (the big KV-cache) is the linear activation (cheap zero-pad on the contraction dim, NO transpose/pack); only Q^T is packed. NOT a special HW mode — a standard int8 matmul with operands arranged so the un-packed tensor is A. | **PROVEN (direct)** | `scorest_probe`: `scores^T[j,i]` vs CPU `QK^T[i,j]` **bit-exact, 0/16384**. | **Eliminates the K^T densify+pack** (the expensive transpose part of the host bridge) — decisive for DECODE: the growing KV-cache K stays linear, only the tiny `Q^T[d,1]` (== Q in memory) is packed per token. Prefill: neutral (Q^T pack ≈ K^T pack cost). Downstream transposes (softmax over Nk = rows of scores^T; A·V becomes `V`-weighted). A·V still needs V cube-packed (straight tile, no transpose). Much cheaper + lower-risk than the reshape-RE pack op. | — |
| **HW-CHAINED full attention core `[QK^T → exp → reduce(Σ), e·V]` in ONE submit (task #20 target)** | extend the coalesced `run_chain_i8_ffn_exp` graph: t0 `QK^T`→ t1 `exp`(SDP, reads t0)→ t2 `reduce`(e·ones→Σ, reads t1)→ t3 `e·V`(reads t1). `e` feeds BOTH the reduce and A·V on-chip (multi-consumer `in0`). Deferred normalize: `attn = av/Σ` (out_scale cancels). | **PROVEN (direct)** | `chainav_probe`: rc=0, ONE submit; `attn=av/Σ` vs CPU max\|err\| **0.0092**, mae 0.0021, COHERENT (0/4096). | The whole attention core (QK^T + softmax + A·V) is now a SINGLE hardware-chained submit — scores/e never leave the NPU. Builds directly on the `[QK^T→exp→reduce]` chain (row below). Same constraints (K%512 via pad, N≤nmax, single-slice). Remaining for a full resident layer: RMSNorm/QKV/rope front + O-proj/residual back (already resident as SW chains) fused in, int16-LUT exp quality, daemon-served. | — |
| **biased fused attention core — scalar-max-biased exp makes `[QK^T→exp→reduce,e·V]` correct on REAL scores (task #20)** | `ork_i8_mm_run_chain_ffn_exp_biased` bakes a scalar `max_bias` into the exp LUT (`silu_build_curve_biased`): `e=exp((score-max_bias)·in_scale)/out_scale`. Any `max_bias ≥ every score` ⇒ args ≤0 (int8 exp never saturates); the bias cancels in `av/Σ`. Plain `ork_i8_mm_run_chain_ffn_exp` now delegates (bias=0, identical). | **PROVEN (direct + orkd)** | `orkd_attn_probe` (real mixed-sign scores, bias=global score max): rc=0, in_scale=0.03125 → attn max\|err\| **0.0096 COHERENT** (0/4096); plain exp (bias=0) saturates **48%** of the same scores (proves the bias is needed). | Removes the "scores must be ≤0" wall for the fused softmax on real decode scores WITHOUT a live per-query max — the scalar bias is a static per-model calibration (int8 score-ceiling, or a measured max). **LUT-cache fix:** the builder rebuilds the exp curve IN PLACE at one static address, so it NULLs `chain_lut_p[*]` on rebuild → `orki_i8_run_chain_impl` reloads the per-core SDP-SRAM LUT (else a per-call-VARYING in_scale/bias serves a STALE LUT — measured 0.098 fresh vs 0.83 as a 2nd call). `in_scale` is the accuracy knob (lower = softer = finer int8 on diffuse data). | — |
| **N attention chains fanned ROUND-ROBIN across cores (biased) (task #20)** | `ork_mm_run_chains_rr_biased` = `ork_mm_run_chains_rr` + the biased exp curve + a once-before-dispatch per-core LUT invalidation (NULL all `chain_lut_p[]` single-threaded; each worker's `orki_i8_run_chain_impl` reloads ITS core's SDP-SRAM LUT at the front of its chain). Fans `Hkv` kv-head fused-attn chains across the NPU cores from one dispatch. | **PROVEN (direct)** | `chainrr_biased_probe`: 3 DISTINCT-data chains, own scratch, shared (in_scale=0.03125, max_bias=global max) → ALL COHERENT (max\|err\| ~0.012, 0/4096 each). Distinct data + all-coherent ⇒ no cross-core LUT/weight leak. | Local NPU only (returns -3 under `c->daemon`); the daemon calls it on its own direct ctx. Per-core Lrc/Lsc buffers make the reload concurrency-safe. | — |
| **ORKD_ATTN_RR — biased attention fanned RR across cores THROUGH orkd, one round-trip (task #20)** | proto msgs 57/58 + `struct orkd_attn_rr` (nchains + shared dims/requant/exp params; payload = nchains `{wkt,wones,wv}` id triples then chain-major Q) → `orkd_attn_rr_i8` (client) / `handle_attn_rr` (daemon: per-chain 4-task chains → `ork_mm_run_chains_rr_biased`). | **PROVEN (orkd)** | `orkd_attn_rr_probe`: 3 distinct chains via orkd → ALL COHERENT (max\|err\| ~0.012, 0/4096), rc=0, no wedge. | **`orkd_warmup` extended to prime ALL cores** (a single-task int8 M=32,N=512 matmul → run_multicore) — else the first RR dispatch fans onto a never-online core and WEDGES (warmup previously primed core 0 only). This is the decode attention path: `Hkv` chains on separate cores concurrently, one round-trip, `e` never leaves the NPU. Remaining: the ggml decode handler builds the `Hkv` chains + calls the route (ORK_ATTN_FUSED) + PPL gate. | — |
| **HW-CHAINED softmax numerator `[QK^T → exp → reduce]` in ONE submit (task #20 target)** | `ork_i8_mm_run_chain_ffn_exp`: the coalesced data-dependent chain (`ork_chain_op`) with EXP fused on the kind-2 SDP task — t0 `QK^T`(matmul, K-pad 128→512)→ t1 `exp`(SDP, reads t0 on-chip)→ t2 `reduce`(matmul, reads t1 on-chip). SDP task reuses the score weight for N-sizing (FFN-chain convention). | **PROVEN (direct)** | `chainexp_probe`: rc=0, ONE submit, scores+e never leave the NPU; Σ vs CPU int8 exp-sum max rel-err **2.97e-2, COHERENT** (0/32 rows). e[0]=12=exp(-2.375)·127 ✓. | **This is the SW-chain → HW-chain step: the N-submit resident softmax numerator collapses to ONE coalesced submit (amortizes the ~2ms/op tax).** Built on `run_chain_i8_ffn` (proven FFN HW chain, task #12) — the exp variant is a 1-line curve swap (`silu_build_curve(exp_f,…)`). int8-LUT fused-exp accuracy ~3% (out_scale cancels in P=e/Σ; int16 LUT is the quality follow-up). Constraints: K%512 (K-pad ✓), N≤nmax, single-slice. Next: fold A·V into the same chain; wire as the daemon-served resident softmax. **Citation corrected 2026-08-20:** this row used to also name a public exp-LUT-builder wrapper that played no part in the proven path — `chainexp_probe` calls only the chain entrypoint above, which builds its curve internally via `orki_silu_build_curve_biased`. The wrapper had no caller anywhere in the repo, its tools, its examples or the fork, and was retired (see MODULARIZE_PLAN.md "retiring the dead exports" for the symbol name and the recovery commit). STATUS UNCHANGED — still PROVEN, same probe, same evidence; only an inaccurate citation was removed. | — |
| **matmul-after-SDP stale-`c->task` bug (root cause of the resident int8 softmax reduce garbage) — FIXED** | run()'s SW Bb-tile matmul path only refreshed `c->regcmd` and RELIED on `c->task` persisting as a matmul from a prior submit; a preceding SDP/LUT op (`exp_i8`) overwrites `c->task` (enable_mask=0x1d + its own regcmd), so the following SW matmul ran the SDP task → stale/garbage output. **FIX:** run() now sets its matmul `c->task` explicitly (enable_mask=0xd, regcfg 108, regcmd_addr=regcmd.dma) before `submit1_db` — like the doorbell already does. **No reset, no re-warm → fully performant** (the doorbell dodged this precisely because it sets its task fresh). | **PROVEN (direct)** | `swreduce_probe`: T1 (MM_I8 K=64 alone) OK; T2 (MM_I8 K=64 **after exp_i8**) was garbage/crash, now **max\|err\|=0**. Regression: `minimal_i8mm_probe` int8 bit-exact across K=32..1024. Reproduced+fixed DIRECT (escaped the orkd-over-ssh wall). | The bug hit the SW path (K%512≠0 or M>64); the doorbell path (int8reduce K=512/M≤64) was immune. Applies to any SW matmul after an SDP op (broader than softmax). Next: confirm the resident int8 softmax seq now composes through orkd (was blocked by this). | — |
| resident int8 softmax seq assembly `[MATMUL_REQUANT_I8 → EXP_I8 → MM_I8]` (task #20) | compose the three proven pieces (K-pad int8 QK^T → scalar-max-biased exp → int8 Σ-reduce) into ONE `ork_submit_seq`, scores/e A2-resident | **reduce garbage ROOT-CAUSED + FIXED (see the matmul-after-SDP row above); orkd wedge solved earlier** | `softmax_i8_seq_probe`. DIRECT: scores bit-exact (1 LSB) but wedges (int8-LUT-after-matmul trap → hang → `sudo reboot`). ORKD (N=64): **rc=0, NO wedge** — orkd's hardened path runs exp-after-matmul without hanging — but reduced Σ is garbage (direct Σ≈1, orkd Σ≈2e8 ⇒ the terminal MM_I8 reduce isn't writing its output under the resident chain). | **(B) orkd routing: the wedge is SOLVED** — `MATMUL_REQUANT_I8` routes through orkd (`reqi8_probe` ORKD K=512 → 1 LSB; the old "-3 TODO" was STALE, daemon-resident weight has `Bf` now), and the orkd-served seq does NOT hit the direct-mode exp-after-matmul wedge. **ROOT-CAUSE ISOLATED (daemon instrumentation, `ORKD_SEQ_DEBUG` in handle_seq + `softmax_i8_split_probe`):** the softmax logic is SOUND — the bug is in the **orkd SW-`run()` path for `MM_I8`**. Daemon seqdbg proved: Stage A forwards scores correctly (op1 Asum=-73 = op0 Csum=-73) and produces CORRECT `e` (Csum(i8)=252479, avg~62); the reduce RECEIVES the correct `e` (Asum(i8)=252479) but outputs GARBAGE Σ (uninitialized-looking int32, ss[0]=-67108106). Cross-referencing configs against `seq_hw_ok` (M≤64 && K%512==0 && Sn==1 → doorbell, else SW `run()`): **doorbell path works through orkd** (`int8reduce_probe`, M=64/K=512), **SW-`run()` path is BROKEN through orkd** — Stage B (M=64/K=64, K%512≠0→SW) garbage, N=512 chain (M=512>64→SW) garbage. **Direct SW-`run()` is CORRECT at all K** (`minimal_i8mm_probe`, plain MM_I8 bit-exact K=32..1024) — so it's specifically the SW path ON THE DAEMON. Same weight (`handle_pack`→`ork_i8_mm_pack`, non-orkd local weight) and same `run()` code as direct, so the defect is in the daemon's environment/dispatch for the SW branch (cores=3, warmup state, or transport of the SW-path output), not the math. **FIX DIRECTIONS:** (a) force int8 seq matmuls onto the proven doorbell path — M≤64 tiling + K-pad to %512 (the user's auto-pad idea + M-tiling); ONLY covers M≤64 (prefill M>64 still SW-path). (b) fix the daemon SW-`run()` path (the general fix). **(b) FURTHER NARROWED (fresh-board, deterministic seqdbg to /tmp/orkd-seqdbg.log via `ORKD_SEQ_DEBUG`):** the reduce `run()` returns rc=0 but `c->Cc` retains STALE data — its seqdbg `C[i32:0..3]` is byte-identical to the PRIOR op's `scores` buffer (malloc-reuse leftover), so `run()`'s `memcpy(C,c->cres,need)` copied a `c->cres` that was never overwritten by the reduce's matmul submit. So the reduce's NPU submit didn't compute/write `c->Cc` in the daemon's resident-chain context. **NOT multi-core** — `ORK_NPU_MC=1` (single-core) still garbage. Prime suspect: matmul-after-SDP-LUT mode transition (the reduce follows `exp_i8` in the chain), but the standalone-reduce control (`minimal_i8mm_probe` through orkd, no preceding exp) could not be captured — it wedges the daemon into **D-state (uninterruptible, unkillable, holds the NPU)**, forcing a reboot. **META-BLOCKER = orkd observability/lifecycle over ssh:** empty output capture (daemonized orkd holds ssh stdout), D-state daemons that need a reboot, and `sudo reboot` stuck (degraded systemd → use a kernel `sysrq` reboot: `echo 1>/proc/sys/kernel/sysrq; echo b>/proc/sysrq-trigger`). **Next session MUST first make orkd debug iteration reliable** (probe output→board file; deterministic seqdbg log — DONE; a way to run/kill the daemon cleanly), THEN submit-level trace the matmul-after-SDP reduce. LESSON: never leave orkd lingering between runs (stale-daemon confound). | — |
| `MATMUL_REQUANT_I8` seq adapter (int8 matmul→int8 out) | wire `ork_i8_mm_run_out8` into `ork_submit_seq` (mult/shift via `o->mult`/`o->shift`) | **PROVEN (direct + ORKD, K≥512)** | `reqi8_probe` → max\|err\| 1 LSB at K=512 direct AND ORKD (K=64 FAILS: requant needs K≥512, registry `set_i8_out8`). ORKD Path B routes (daemon-resident weight has `Bf`; the earlier "-3 TODO" was stale). | For int8 K≥512 matmul→int8 chains (FFN-style); NOT usable for the small-K (K=32) x-max broadcast (that stays f16). Confirms an all-int8 softmax island is NOT clean — small-K broadcasts want f16, exp wants int8/int16, int8-requant wants K≥512 → the full resident island is inherently mixed-dtype, a multi-session integration | — |
| `ork_i8_mm_run_chain_ffn` (9009) | GENERAL FFN chain: per-task OP_MM32/MM8/SILU/EWMUL, SDP reads prior by index | **PROVEN** | `chain_gu_silu_probe` → "FULL FFN INNER CHAIN WORKS (gate,silu,up,glu,down, ONE submit)" | ops[].in0/in1 aliased; MM8 when feeding SDP; single M-tile/task. Reads matmul output **LINEAR** in-chain. | the coalesced FFN inner |
| `ork_i8_mm_run_chain_gsilu` (8980) | chain with FUSED int8 SiLU on gate task | **PROVEN** | `chain_gu_silu_probe` → "REAL-WIDTH FFN INNER … ONE submit, EXACT" | built on run_chain_i8 + set_i8_silu | quality note: int8 fused silu inaccurate |
| `ork_i8_mm_run_chain` / `orki_i8_run_chain_impl` (8961/9020) | batch S>1 independent int8 matmuls, one submit | **PROVEN** | `moe_expert_probe` → chain correctness PASS | per-task M ≤ chain cap; routes to orkd | — |
| `ork_i16_npu_chain_gatesilu` (8217) | data-connected int8-mm(int16-out) → int16-silu bridge | **PROVEN (small-shape)** | `chain_i16silu_probe` → "BRIDGE WORKS" | K%32,N%32, N≤nmax, M≤64, N&7; G(matmul int16-out) is silu's EWCUBEH input. | uses `set_i16_out` |
| `ork_f16_npu_chain_mm_perchan` (8424) | fp16 mm → fp16-out → vendor per-channel SDP (attn A·V) | **PROVEN** | `mm_perchan_f16_probe` → "ALL OK — bit-exact, on-NPU" | needs 2-pass cold re-warm (`OCK_HW`) else zeros; `orki_f16_set_out_fp16in` + `REGCMD_MUL_F16_CHAIN` | the working fp16 chain |
| `ork_dyn_begin_mc` / `_colsplit` (9972/9835) | int8 multi-core doorbell (M-tile/K-split/N-colsplit auto) | **PROVEN** | `ork_dyn_ntile_test` → "OK (bit-exact)" M=1 & M>1 | bit-exact ceiling `mg_max*64`; ~1/4000 dispatch-drop auto-recovered | — |
| `ork_i4_dyn_begin_mc` / `_grouped` (9648/9738) | int4 W4A4 M=1/decode doorbell (int16 out) / float-grouped int4 | **PROVEN** | `i4_doorbell_probe` → "DOORBELL PASS — bit-exact"; grouped gtest maxerr 0.0000 | int16-sentinel clean-before; Sn>1 & Sk≤16 (grouped Sk≤256). Serves decode + non-batch; M>1 single-slice prefill goes to `run_i4_bchain_db`. **DISPATCH FAILURE (root-caused 2026-08-25, kernel watchdog):** a "miss" is the submit being accepted (`rc=0`) while the hardware **never executes a task** — PC completed-task counter stuck at `0x0` with the job >100 ms old, on all three cores. The old "~1/4000 dispatch-drop" figure is an AVERAGE OVER A BIMODAL POPULATION and is misleading: 12/12 observed misses were one of TWO shapes (`K=17408 N=5120 Sk=2` = the 27B ffn_down, and BCHAIN `N=8192`), zero elsewhere across ~1.1M programs/run — per SUBMIT the real rate is ~1/500. **Quote the shape, not a rate.** The contiguous-tail sentinel pattern is NOT a partial write: cores get contiguous op ranges, so a core that never runs leaves its slice at the sentinel. Reproducer `tools/re/i4_widek_stall_probe.c`; full write-up + ruled-out list on the wiki (NPU-Quirks) | — |
| `run_i4_bchain_db` / `bch_db_worker` (npu.c) | int4 W4A4 **M>1 PREFILL** doorbell — H-row native batches (`synth_i4` mc>1; H from the MEASURED `orki_i4_hcap`) + bank-width `Wb=131072/K` N-tiling + weight-once chaining, on the NONBLOCK self-healing spine, PIPELINED across the npu_pool (one worker/core: build→submit→poll→de-tile) | **PROVEN** | `i4` M=128/256/512 K512 batch shapes → maxerr=0; A/B (K2048, warm) M128N2048 4.05 / M128N6144 11.9 / M256N2048 8.21 ms — **beats the (removed) blocking BCHAIN** (4.17/12.2/8.39) and ~24–25× over the per-row doorbell | DEFAULT for M≥2, Sk==1, Sn==1, N%64==0 (else → per-row `ork_i4_dyn_begin_mc`). **H is a MEASURED TABLE, not a formula (`orki_i4_hcap`, i4/chain.c)** — 25 probed K (32..8192); no closed form fits them and interpolation is unsafe BOTH ways — below K=256 the envelope goes FLAT (128 for K<=128, 64 for 128<K<256) with no relation to 16384/K, which wants 512 at K=32 (`ceil(16384/K)` overshoots at K=3072/3584/5120 and undershoots at 6144; K=768 and K=3072 share frac .33 yet round opposite, 22 vs 4). Unmeasured K fall back to `floor(12288/K)` — an empirical 0.75x margin, not a guarantee. An overshoot does NOT just miscompute: at K=3072/3584 the submit is accepted on every core (`submit_rc=0`) and the NPU never starts it (`hw_elapse=0`), so the recover loop resets 6× and fails after ~15 s — this was the "native W4A4 hangs at prefill" report (fixed 2026-08-21). Grade a candidate H by CHECKSUM only: `rc` misses the silent-miscompute mode and timing misses it too, and check a new entry BOTH standalone AND as a slice remainder (K=8192+rem). **FIXED 2026-08-22 (K=2560 slice-remainder non-determinism):** K=2560 agreed six ways STANDALONE at H=7 and is correct there, but as a slice remainder H=7 varied run-to-run on ~40-60% of runs — K=18944, the real 7B ffn_down, is 2x8192+2560. Per-sub-tile checksums isolated it to the 2560 tile alone (the 8192 tiles were bit-identical every run); not the accumulate, not recover, not an early poll, not an overrun. The rate falls smoothly with H (7:~50% 6:5/8 5:4/8 4:0/8) => stale-CBUF-residue exposed by a marginal H, so 2560 is now 4 (where 2816..4096 already sit). **Every entry must be validated BOTH standalone and as a slice remainder (K=8192+rem); standalone is the weaker test and a single agreeing run would have passed H=7.** All 25 entries re-confirmed as remainders, 6 runs each. MECHANISM UNKNOWN (an earlier "kernel timeout + non-idempotent retry" claim was WRONG — read from one sample; a second has corrupt runs at the baseline +1 reset). Ruled out by measurement: our recover loop (MC-RECOVER=0), submit rejection (0), extra soft resets (+1 only), the submit itself (`dyn_end` done=3/3 in 496 us), our submit timeout (wall flat across 500..4000 ms), an early poll (2 ms dwell no help), an overrun (padding `part` no help). ROOT-CAUSED 2026-08-22: the host-side stall is `ork_dyn_end`'s completion poll burning the full `i4batch` 2 s `miss_to` on REPEAT submits, and that stall is NECESSARY for the corruption (every wrong run has the 2560 tile at 2 s; no fast run is wrong). **Underlying defect is in the SHARED drain, not int4:** `ork_dyn_progress` returns the HIGHEST landed index, not a count, and `ork_dyn_end` tests `last >= h->S-1` — so a HOLE at a non-final task reads as success and de-tiles partial output (`[dyn_end] done=2/3 last=2 ... INCOMPLETE` on the corrupt runs), while the recover loop and auto-dump both sit in the same `else` and never fire. FIXED 2026-08-22 in `ork_dyn_progress` (stop at the first hole — every caller reads it as a FRONTIER: dump names `prog+1`, `ork_dyn_remaining` uses `P-(p+1)`, queue halts at `prog+1+HEADROOM`). The recover loop was never broken, just UNREACHABLE behind the faulty test — with the K=2560 H=7 bug reintroduced, `test_slice_rescue` now PASSES 3/3 with `[MC-RECOVER] reset + resubmit` where it failed 3/3 before. Validated: `make test` PASSED at default + `ORK_SSM_KEEPWARM=0` + `ORK_MIXED_NOTHRASH=1`, reset count 51 == baseline 51. (`mode_probe` unusable as a gate — hangs at BASELINE, `rknpu_gem_fault`; see wiki.) H=7 was never a win regardless: **2.1 s/run vs 1.0 ms at H=4**. Regression-tested by `test_slice_rescue` case `i4-rem2560` (K=18944 = 2x8192+2560, 5 runs, all must be bit-identical — a single golden compare passes ~50% of the time with the bug, so repetition is the test). Parallel build+de-tile (pool); seeds/polls the WRITTEN strided int16 cell-set PER CACHE-LINE (per-element `dc` was a ~60ms host wall; per-program `validate_regcmd` gated to `ORK_I4_VALIDATE`, was the wide-N tax); recovery serialized on the caller after join (global RESET can't run with live cores) | supersedes STRATEGY-A/C, blocking BCHAIN, CBATCH (removed #51/#52/#53) |
| `ork_i16_npu_chain_mm_perchan` (8325) | int8-mm(int16-out) → int16 **2-input per-channel** SDP | **DEAD — HANGS (errno 110)** | `chain_mm_perchan_probe` → 176/512, hangs; (2026-07-14) "int16 2-input SDP `REGCMD_MUL_I16` is NOT chain-safe — keeps BS-ALU active `0x4040=0x20050`" | Dtype-match necessary but NOT sufficient; only fp16 has a chain-safe 2-input SDP. | **use fp16 (`chain_mm_perchan_f16`)**. NOT the int16 SiLU path (that one works — don't conflate). |
| `ork_i16_npu_chain_mm_silu` (8051) | Phase-0 walk-proof (data NOT connected) | WIP (walk-proof) | proves the chain WALKS; buffers separate | — | superseded by `gatesilu_i16` |
| `ork_npu_chain_progs` (h:573) | general PC-chain core (address-aliasing DAG) | **DEAD / superseded** | `CHAIN_ASSEMBLER_WIP.md`: "abandoned … never worked end-to-end. Do not build on it" (no passing probe) | kept only for 8 RE probes | **use `ork_i8_dyn_begin_seq` / `ork_submit_seq`** |

## SDP ops (activation & elementwise, enable 0x18)

| name | purpose | status | probe → verdict | gotchas |
|---|---|---|---|---|
| `ork_i16_npu_silu` (+gelu/rsqrt/exp_i16) (8493) | int16 activation via LUT | **PROVEN (accurate)** | HW-chained (2c2f9b9), "max\|err\|=75 RKNN-class"; ggml note "~325× more accurate than int8 fused (0.28 vs 92 @gmax132)" | atom-8 EWCUBEH (N%8), dslot 138; lazy idx calibration wedges if FIRST op after multi-core matmul → `silu_calibrate_idx16` |
| `ork_i8_npu_silu`/`gelu_i8`/`rsqrt_i8`/`exp_i8` (7995+) | int8 activation via LUT | **PROVEN** | `test_activations` PASS, "max\|err\|=2" | atom-16 ORK_SEQCUBE (N%16) |
| `ork_i8_mm_run_silu` (5140) | gate matmul + fused SiLU | **PROVEN bit-exact** | `fused_silu_test` → "VALIDATED"; `fused_mtile_check` OK | per-TENSOR quant; rk3588-gated |
| `ork_i8_npu_ewmul`/`_f16` (6331/6375) | 2-input elementwise multiply | **PROVEN** | `ewmul_probe` PASS | `_i16` variant EXPERIMENTAL (int16 not bit-exact over signed range) |
| `ork_i8_npu_add`/`_f16` (7174/7190) | 2-input add | **PROVEN** | domain-propagation fix 2026-07-20 (no dedicated add probe; via `make test`) | `_i16` (7257) EXPERIMENTAL — "int16 add NOT bit-exact over signed range" |
| `ork_f16_npu_mul_perchan`/`_i8` (6684/7085) | per-channel scale | **PROVEN** | `bs_scale_probe` → "per-channel scale (i8+fp16) WORKS at general geometry" | fp16 contig vs atom-8 cube variants |
| `ork_f16_npu_mm_perchan`/`_fused`/`_diag` (6586/6033/6619) | fp16 mm → per-channel scale | **PROVEN 512/512** | `mm_perchan_f16_probe`/`_diag` → "ALL OK — bit-exact" | composes f16_mm_f16out + mul_perchan_f16 |
| `ork_f16_npu_replay_softmax` (7490) | replay vendor 9-task softmax verbatim | **PROVEN (replay)** | "replays bit-exact, task_number=9, rc=0"; `softmax_reduce_probe` → "exp→reduce WORKS" | fixed capture geometry (N=64, 256 rows) |
| `ork_i8_npu_row_max` (6535) | row-max reduction | **PROVEN** | `max_reduce_probe` OK | — |
| `ork_i8_mm_run_ewmul` (5529) | up-matmul × G fused in SDP output | **DEAD — WEDGES** | `fused_silu_test` → "fused EW-mul WEDGES (DPU_RDMA graft wall)"; opt-in `ORK_TEST_EWMUL` | **use standalone `ork_i8_npu_ewmul`** |
| `ork_f16_mm_run_silu` (5360) | fp16 gate matmul + fused fp16 SiLU | **DEAD — not viable** | "garbage PPL 9072 vs 9.15 int8; gated OFF" | **use `ork_i16_npu_silu` or CPU silu** |
| `ork_npu_requant_perchan_i32` (7026) | standalone SDP int32→int16 per-channel requant | **PARTIAL — loopback not viable** | (2026-07-15) "SDP clamps to 2-byte read → errno 110"; over-fetch fix rc=0 "BUT processes as TWO int16 lanes, not true int32" | int16-range accumulators usable (even-channel low half); full int32 needs lane recombine (open) |
| `ork_f16_npu_replay_reshape` / `rope_neox_f16` (6856/6424) | reshape/permute, NEOX RoPE fp16 | WIP | `reshape_probe_f16`, `rope_probe` | RE-stage |

## sequence scans (linear attention / SSM, `src/npu/ssm.c`)

The chunked form of a linear-attention recurrence: the intra-chunk part becomes a batch of small
matmuls the NPU runs as a pooled 3-core fp16 stream, while the inter-chunk carry stays sequential on
the CPU. Whether that trade wins is a **scale** question, and the two scans answer it differently —
which is the reason both rows exist. Do not generalize one to the other.

| name | purpose | status | probe → verdict | gotchas | use / notes |
|---|---|---|---|---|---|
| `ork_ssm_scan_f32` (`ssm.c` 182) | Mamba-2 / SSD chunked scan; matches `ggml_compute_forward_ssm_scan_f32` | **PROVEN** | `test_ssd_chunk_npu` (in `make test`) → byte-coherent vs the CPU reference; model-level ~**2× at Q8_0 2.7B** (9.72 vs 4.37 P128, 18.0 vs 8.89 P512) | **Crossover is real and the small-model verdict inverts.** An earlier "no crossover" reading came from 130M + Q5_K, where CPU-dequant projections diluted the scan out of the measurement. CPU recurrence craters ~33× with scale (cache-bound); NPU degrades ~11× (submit floor amortized over a growing count of tiny matmuls). | Enabled fork-side by a SIZE GATE, not here: `ggml-ork.cpp:9844` keeps `nh<64` (≈<1.5B) on the CPU. Knobs `ORK_SSM_CHAIN` (default 1 — fused-multicore fp16 stream), `ORK_SSM_CS` (default 128), `ORK_SSM_KEEPWARM`. |
| `ork_gdn_scan_f32` (branch `feat/gdn`) | Gated DeltaNet scan (Qwen3.5 / Qwen3-Next / Ornith) | **WIP — coherent, not a win** | **no probe in this tree** (the chunk validator and its per-stage profiler live on `feat/gdn`, so this row cannot be re-verified from `main` — check it out first) → coherent end-to-end in a real Qwen3.5 model, but **loses ~1.9× @0.8B, ~4.8× @9B** (gap WIDENS with scale) | **The SSD result does not transfer.** GDN's chunked form adds a CPU UT-solve (**38%** of scan time) that the CPU recurrence simply skips, plus 44% NPU submit; and GDN state fits cache, so the CPU side never craters — **no crossover exists to cross**. | NOT on `main`. Parked on `feat/gdn` (+ `GDN_ON_NPU_WIP.md`). Marshaller machinery it produced — weightless-op offload, densify, state-transpose/GQA — was reusable and did land. |

## ggml FFN handlers (env-selectable, `ggml-ork.cpp`)

| flag | purpose | status | probe / evidence | notes |
|---|---|---|---|---|
| (default) | int8 gate matmul + **all-CPU fp32 SiLU** | **PROVEN / shipped** | "all-CPU-silu is the shipped default"; PPL-validated | `fc.silu_cpu = !(GATE_F16\|\|F16)` |
| `ORK_FFN_SILU_I16` (328) | int8 gate matmul + on-NPU int16 SiLU | **PROVEN** | "~325× more accurate than int8 fused LUT (0.28 vs 92 @gmax132)"; PPL A/B | the accurate integer silu path; gate `ORK_FFN_SILU_I16_GMAX` |
| `ORK_FFN_F16` (316) | ALL-fp16 FFN inner (no int8 activation quant) | **PROVEN** | coherent (~18 PPL); gate `ORK_FFN_F16_GMAX` | 2× IOVA → ~5 layers/4GiB; N-chunked (24MB single tile fails under IOVA frag) |
| `ORK_FFN_F16_JIT` (333) | host int8+bscale inflated into shared fp16 scratch (W8A16) | **PROVEN** | `jit_inflate_check` → "PASS (== direct fp16 pack, bit-exact)" | decouples fp16 layer count from IOVA cap |
| `ORK_FFN_F16_CPUSILU` (340) | plain fp16 gate matmul + EXACT CPU silu | **PROVEN** | "plain matmul + fp32 CPU silu is exact"; PPL-validated | needs `ORK_FFN_F16` or `ORK_FFN_GATE_F16` |
| `ORK_FFN_SILU_CPU_GMAX` (324) | high-gmax layers: per-channel gate + CPU silu | **PROVEN** | "baseline quality, ~2.3× fused-silu gate cost" | per-layer policy |
| `ORK_GU_CHAIN` (281) | HW-chain up+gate into ONE run_chain_i8 submit/M-tile | **PROVEN** | "falls back to per-op on any failure, output always valid" (no unit probe; runtime fallback-guarded) | K%512 & ≤4096, Nff single-slice |
| `ORK_FFN_CHAIN` (292) | per-tensor SwiGLU chain, int8 intermediates on-NPU | **PARTIAL / WIP** | "**disabled under orkd (fd-local ops)**. Open work (#20) = route through orkd `ORKD_SEQ`" | needs SmoothQuant per-layer; M<32 skip |
| `ORK_FFN_GATE_F16` (310) | fp16 gate + fused fp16 SiLU | **PARTIAL** | fused fp16 silu not viable unless `_CPUSILU` set (no probe) | — |
| `ORK_GATE_ABLATE` (4250) | diagnostic: isolate gate's 3 error sources | diagnostic | — | unset = normal |

## CPU native kernels (`include/ork_native_cpu.h`)

| kernel | purpose | status | probe / evidence | notes |
|---|---|---|---|---|
| `ork_cpu_gemm_i4` / `ork_cpu_gemm_nf4` | batched (M>1) int4 / NF4 CPU expert GEMM — 4×4 register-blocked, weight+unpack amortized across rows (the M=1 `ork_cpu_gemv_m1` re-unpacks per row). The MoE-**prefill** accelerator for the ggml-ork CPU cold path. | **PROVEN (bit-exact)** | `test_i4_gemm` (in `make test`): BIT-EXACT vs M separate `ork_cpu_gemv_m1` calls on-board (NEON dotprod); speedup vs M=1 gemv **I4 2.3× / NF4 1.85×** (NF4 faster in absolute time — 1-op `vqtbl1q_s8` unpack vs int4's 2-op sign-extend). | Drives the MoE AUTO-PROFILE (no env knobs): **prefill 36.9 t/s = 1.59× native ggml** (experts NF4-on-CPU + attn int8-on-NPU), PPL 10.77 vs native 10.21. |
| `ork_dot_nf4` / `ork_dot_nf4_x4` PRFM prefetch (`ORK_PRF_DIST`, default 64 = one cache line) | hide DRAM latency in the M=1 (decode) weight stream, which is read once (no reuse) so every K-step stalls on the next 16B nibble block. | **PROVEN** | `test_nf4_decode` (DRAM-cold, 32 MB > L3): NF4 70.6→64.9 µs/expert (**+8.8%**), I4 100.3→90.4 (**+11%**) at DIST=64; 128/256 WORSE; LOC KEEP==STRM. Bit-exact (`test_i4_gemm`). | The M=1 loop is **latency-bound, not bandwidth-bound**: one A76 achieves ~8 GB/s of the ~21.9 GB/s it can stream (~22% of vdotq peak) — hence the win. DECODE-only: prefill is flat (weights already amortized over M rows). |

## ggml attention offload (`ORK_ATTN`, ggml-ork.cpp) — CORRECT as of 2026-08-19; still OFF by default (perf-neutral)

| op / path | purpose | status | probe / evidence | notes |
|---|---|---|---|---|
| `ggml_backend_ork_flash_attn_ext` (the `ORK_ATTN` gate that fires when llama.cpp has flash-attention ON) | claim `GGML_OP_FLASH_ATTN_EXT` and run QK^T / A·V on the NPU (fp16), softmax in between | **CORRECT (fixed 2026-08-19)** | `fa_diff_probe` — differential vs the ggml **CPU backend on the same graph** (scratchpad; builds one FLASH_ATTN_EXT node, computes it on CPU then on ORK, diffs elementwise + per-head + per-query). DK=DV=256 H=16 Hkv=2 over n_kv ∈ {512, 1024, 2048} × nb ∈ {64, 128, 256, 384, 512}, plus DK=DV=128 H=16 Hkv=4 n_kv=1024: **NRMSE 2e-4 (pure fp16 rounding), 0 elements out of tolerance**. Before the fix: NRMSE 30–153, first bad output at query **352** (n_kv 512) / query **0** (n_kv 1024). | The defect was **NOT** the handler's CPU-side math — that was always correct (`ORK_ATTN_CPU=3` PASSES at every shape, NRMSE 2e-4). It was an **M-ENVELOPE violation**: the handler passed `M = n_tokens` straight into `ork_f16_mm_run_stream_chain`, which synths ONE regcmd program per task and is only valid inside the 0x1040 K-reduction schedule's tile `mg_max(K)*64` (npu.c `run()` tiles to exactly that; the stream/chain primitives do not, and are documented "single M-tile"). That is **704 rows at K=DK=256** (QK^T — fine at ubatch 512) but **320 at K=nkvp=512** and **128 at K=nkvp=1024** (A·V — silently wrong past the tile; `ORK_ATTN_CPU=2`, A·V on CPU, PASSES). Fixed with `ork_f16_mtile()` + `ork_attn_chain_mtiled()` in ggml-ork.cpp. **Perf verdict unchanged — keep OFF by default:** offloaded attention costs 1422 ms of which only 417 ms (29%) is NPU matmul; 1005 ms (71%) is CPU densify/repack + softmax + scatter, so marshalling costs ~2.4× the matmul time saved (pp64 31.84 → 30.56 = 0.96×), and NPU matmul is only 11.6% of the pp512 wall. |
| `ggml_backend_ork_bmm_fp16` (`ORK_ATTN` batched/dynamic `MUL_MAT`, `ne[2]>1 && M>1`) — the gate that fires when flash-attention is **OFF** (attention arrives as `mul_mat` + `soft_max_ext` + `mul_mat`) | run both-operands-dynamic batched GEMMs on the NPU in fp16 | **CORRECT (fixed 2026-08-19)** | `bmm_diff_probe` — differential vs the ggml CPU backend on one `mul_mat` node with `src0`[K,N,Hkv] f16 × `src1`[K,M,H] f32. K/N ∈ {256,512,1024} × M ∈ {64,512} × (Hkv,rk2) ∈ {(2,8),(1,16),(16,1)}: **NRMSE 8e-4 … 1.7e-3** (fp16 rounding of the f32 operand; worst-case 336/2M elements outside a 5e-2 band at K=1024, all near-cancellation rows). Before: **NRMSE 1.42, 98% of elements wrong, every shape**. End-to-end: `ork_ppl <q8attn.gguf> /tmp/ppl_corpus.txt 1024 512` baseline **10.8332** (NLL 2.3826) vs `ORK_ATTN=1 ORK_ATTN_SM_CPU=1` **10.7607** (NLL 2.3759) — **−0.67%**, was **67.8630**; 0 `RKNPU_SUBMIT` failures in both runs, and `ORK_ATTN_TRACE=1` confirms this path executed (`[f16scratch] K=256 N=256` in both resident domains). | **THIS was the PPL 10.83 → 67.86 blowup**, not the FA handler — `ork_ppl` sets `flash_attn_type = DISABLED`, so on that harness `FLASH_ATTN_EXT` never appears and all attention goes through here. Two bugs, both silent: (1) **transposed weight** — the handler filled `B[j] = src0(j%K, j/K)`, which is exactly `src0`'s own ne[0]-contiguous memory order, i.e. `[N][K]`, while `ork_f16_mm_repack` reads `B[k*N+n]` (`[K,N]` row-major). Proven by emulation: feeding the driver `Bᵀ` reproduced the NPU output to NRMSE 1.9e-4 at K=N, K≠N and every M. (2) **stale derived weight** — see the `ork_f16_mm_repack` row below. This is why forcing the FA matmuls to CPU (`ORK_ATTN_CPU=3`) left the PPL byte-identical: that flag does not touch this path. |
| `ork_f16_mm_repack` derived-copy coherence (npu.c) | refresh an existing fp16 `ork_w`'s tiles in place, so a caller can keep a persistent weight slot instead of bcreate/bdestroy per matmul | **FIXED 2026-08-19** | `bmm_diff_probe` Hkv=16/rk2=1 (a distinct weight per batch slice, so every slice needs its own repack): multi-core **NRMSE 1.37, wrong from head 1 onward**; the SAME run with `ORK_NPU_MC=1` **PASSES**. After the fix multi-core PASSES. | The fp16 **multi-core colsplit does not read `Bb`** — it builds a CONTIGUOUS concatenation of the K-slice tiles ONCE (`w->Bbc` for Sn==1, `w->Bbc_ns[]` for Sn>1) behind a `*_valid` latch that was never cleared. So a repack refreshed `Bb` and the multi-core path kept computing against the **first weight ever packed into that slot**, silently. Single-core `run()` and `ork_f16_mm_run_stream{,_chain}` read `Bb` directly and were always correct — which is why the FA handler (one `ork_w` per head, chain primitive) never showed it and `ork_bmm_fp16` (one shared slot repacked per head, `ork_f16_mm_run`) always did. `ork_f16_mm_repack` now refreshes the CONTIG copies in place (sizes unchanged, no bcreate churn). `ork_i8_mm_repack` was already coherent (it refreshes `Bf`; `Bbc` is fp16-only). |
| on-NPU softmax inside the FA handler (`ork_i16_npu_exp` + reduce-matmul Σ + per-channel normalize), and the standalone `GGML_OP_SOFT_MAX` claim | keep the whole softmax on the NPU between the two fp16 matmuls | **DEAD — now OPT-IN (`ORK_ATTN_SM_NPU`); default is the fp32 CPU softmax** | `fa_diff_probe`: with the NPU softmax on, **NRMSE 2e-4 at MR=H·N=8192** but **0.55–0.69 at MR=6144 and MR=4096**, wrong from row 0. IDENTICAL numbers with `ORK_ATTN_TNORM_OFF`, `ORK_ATTN_MAX_CPU` and `ORK_ATTN_SM_SUMCPU` all set — which excludes the transposed A·V, the int8 row-max and the reduce-matmul Σ, and pins it on **`ork_i16_npu_exp`'s own row-count envelope**. | Previously the DEFAULT. Now `sm_npu` requires `ORK_ATTN_SM_NPU`, and `supports_op` no longer claims standalone `SOFT_MAX` unless that flag is set — so plain `ORK_ATTN=1` is correct end to end, including the **MoE expert-router softmax** (which the LUT perturbs: worth ~3% PPL, 70.06 → 67.86). `ORK_ATTN_SM_CPU` is kept as an accepted alias meaning "CPU softmax". **OPEN**: the `ork_i16_npu_exp` M-envelope itself is not diagnosed — a fix there re-enables this path. |

## orkd daemon RPC surface

| name | purpose | status | evidence | notes |
|---|---|---|---|---|
| `ORKD_SEQ` → `handle_seq` | route heterogeneous op sequence through daemon | **PROVEN** | "seq-grp-silu16/silu8 DIRECT + ROUTED … make test ALL PASS, 0 wedges" | one round-trip batches S ops |
| `orkd_run_chain_i8` | fused int8 chain on daemon | **PROVEN** | `orkd_ffn_probe` → routed chain bit-exact; "fused chain through orkd (f97db3f)" | single-domain (tasks[0].w→domain) |
| `ORKD_DOM_REQ` / per-client IOMMU domains | per-client domain on packs/work | **PROVEN (corrected 2026-07-20)** | `domain_correct` PASSES 2-domain int8; `orkd_dom_api` PASS | earlier "kernel broke multi-domain" was a self-induced runtime wedge, NOT real |
| shm ring `orkd_run_i8_ring` | remove per-op socket round-trip | **PROVEN** | `orkd_ring_probe` → "bit-exact + ~1.9× at M=1"; SYNC ring is the real ~1.6× win | async gives ~0 single-client gain (daemon serial) |
| multi-consumer (N clients) | concurrent clients on one daemon | **PROVEN to N=6** | "≥3-client deadlock FIXED (53e46f7), board-proven N=6"; `orkd_2proc` PASS 2&3 | supersedes stale `test_orkd_2conn_seq` "N≥3 hangs" note (#2) |

---

## CONTRADICTIONS / STALE NOTES (resolved by probe verdicts)

1. **`set_i16_out` "abandoned/wedges" (CHAIN_ASSEMBLER_WIP.md) vs "SOLVED, works" (code).**
   RESOLVED: the MD's blanket note is **STALE** for `set_i16_out` itself — it was fixed
   later the same day (`6fb8b9f`; old wedge = `0x4010=0`/N-16 typo precision mismatch).
   `chain_i16silu_probe` → "BRIDGE WORKS." The MD's *other* point — the `chain_progs`
   PC-chain route is superseded by the NONBLOCK doorbell seq — still stands.

2. **N≥3 clients "HANGS" (`test_orkd_2conn_seq` comment, 2026-07-19) vs "FIXED to N=6" (ORKD_WIP).**
   RESOLVED: the probe comment **predates** the `53e46f7` poll/revents deadlock fix. STALE.
   Re-run `test_orkd_2conn_seq 4+` to update the comment.

3. **int16 chain-safety: matmul-int16-out "PROVEN" vs `chain_mm_perchan_i16` "HANGS".**
   NOT a contradiction — two different ops. The matmul **int16 output** is proven; the
   **int16 2-input per-channel SDP** (`REGCMD_MUL_I16`) is the one that hangs (BS-ALU stays
   active). The int16 **single-input SiLU** (`ork_i16_npu_silu`) is PROVEN and chain-safe.
   Don't conflate the three — this trap cost a debugging detour.

4. **fp16 SiLU: partially-live env flags vs "not viable" verdict.**
   The `ork_f16_mm_run_silu` / `SILU_F16` seq target / `ORK_FFN_GATE_F16` fused path exist
   in the API but fp16 fused SiLU is NOT VIABLE (garbage PPL). `SEQ_CLASS[]`'s
   `ORK_OP_SILU_F16` row is a NULL/TODO dead entry. **Do not wire fp16 fused SiLU** — use
   int16 SiLU or CPU silu.

5. **gmax gating scaffolding is inert.** The persisted gmax sidecar/profile has "No active
   consumer yet (shipped policy is all-CPU-silu)." It's foundation for a future selective
   int16-silu path (blocked, #35). Don't assume gmax policy affects the shipped path.

---
*Extracted 2026-07-21 from `src/npu.c`, `tools/*.c`, `ggml-ork.cpp`, and root `*_WIP.md`. Paths updated
2026-08-19: npu.c was split by precision into `src/npu/` — an op now lives in its precision folder
(`src/npu/i8/`, `/f16/`, `/i4/`, `/i16/`), the substrate in `src/npu/core/`. See AGENTS.md §4.
Line numbers drift — grep the symbol. To change a status, re-run the named probe first.*
