# build artifacts
test_matmul
layer
decode
model
llama2
bench
rkllm_bench
ksubmit_probe
regcmd_decode
*.o
*.a
*.so
# test models (downloaded, large)
*.bin
!regcmd_*.h
# editor
.DS_Store

slice_probe

attn_cost

wiki_tmp/

# Local paper-planning notes — keep private, do not publish in advance
papers-to-write.md

# generated API docs (make docs)
docs/api/
tools/vkbw.spv
