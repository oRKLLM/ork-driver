# size_budget.txt — per-file line budgets, enforced by `make check-registry` (check 9).
#
# WHY: src/npu.c reached 15,313 lines because nothing ever said stop. Twelve refactor rounds
# (MODULARIZE_PLAN.md) brought every module under 900. Without a gate that is a one-off cleanup
# that decays; with one it is an invariant, and growing a file past its budget becomes a
# deliberate, reviewable act rather than an accident nobody notices for a year.
#
# Format:  <path-or-*>  <max-lines>  <reason>
# The default (*) applies to any src/**/*.c and src/**/*.h without an explicit entry.
#
# To exceed a budget you must edit THIS file and say why. That is the point — the friction is
# the feature. Prefer splitting: the seam cost is usually far lower than it looks (i8/dyn.c split
# three ways for ZERO crossing symbols), and it is worth measuring before assuming otherwise.

*                       900   default: no single module should need more than this

# --- justified exceptions -------------------------------------------------------------------
# npu.c is the dtype-dispatch SCAFFOLD, not a module: run/run_multicore, the op-sequence
# scheduler, bmm dispatch, async wrappers and ork_npu_init. It is ~55% comment (1,092 code lines
# of 2,891). This budget should RATCHET DOWN as the remaining stranded doc-comments are placed;
# it is set just above the current size so the file cannot grow, only shrink.
src/npu.c              3000   dtype-dispatch scaffold, 55% comment; ratchet down as docs are placed. +25 (2026-08-25, Tier 19): the doorbell spine moved to npu/core/ and its dtype ROUTER moved here, which is precisely what this file is for — the ratchet exists to stop implementation accreting in the scaffold, not to keep dispatch out of it. The spine lost the routing it should never have owned (an int8-labelled file dispatching to int4, which cost a wasted board A/B). Keep ratcheting down. +5 (2026-09-04, TEMPORARY): doorbell phase timers at the ork_dyn_begin_mc/ork_dyn_end call site in run_multicore, for the top-priority M=1 decode bandwidth investigation (the handler gets 3.1-6.3 GB/s where a probe gets 8.8-16 through the identical call; eight external causes already disproven). They must live at THAT call site -- it is the M=1 int8 path -- so there is no better home; the globals and accessors went to core/prof.c. REMOVE these five lines and restore 2975 once the gap is root-caused. +20 (2026-09-04): the ROOT CAUSE of part of that gap, and its fix -- the M=1 int8 doorbell path is NONBLOCK and runs both its host half and its spin-poll on the CALLING thread, but orki_pin_big_core is only reached on the single-core fall-through, so under ggml it ran on an A55 and cost 1.27x end-to-end. The fix scopes a big-core-SET affinity around the call (save/widen/restore) rather than pinning the caller for life, which would collide with ggml's threadpool. It belongs at this call site; the comment carries the measurement.
src/npu/i4/chain.c      955   TEMPORARY (+20, remove with the probe): ORK_DYN_NOCHAIN diagnostic helper orki_i4_submit_maybe_nochain, isolating HW task-chaining as a single variable while root-causing the permanent mid-write int4 doorbell round stall. Delete the helper and restore 935 once the stall is understood. Prior reason: offline (no-device) chain support, plus the predictive-drain estimate in bch_db_worker (+20). The drain is the one place that knows both when an op was submitted and when it landed, so the EWMA sample has to be taken there; moving it out would mean passing the timing back across the worker boundary for no gain. Prior note: the file sat at exactly 900 and the ONE-LINE ork_npu_init_offline hook put it over 900; its loop was already factored into orki_cpu_chain_i4 (core/buf.c) and shared with the i8 twin. Splitting a ~900-line file to absorb these is not the cheaper seam.
